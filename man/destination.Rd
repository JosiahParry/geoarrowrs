% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_dest_rhumb}
\alias{ga_dest_rhumb}
\alias{ga_dest_euclidean}
\alias{ga_dest_haversine}
\alias{ga_dest_geodesic}
\title{Destination from origin, bearing, distance}
\usage{
ga_dest_rhumb(origin, bearing, distance)

ga_dest_euclidean(origin, bearing, distance)

ga_dest_haversine(origin, bearing, distance)

ga_dest_geodesic(origin, bearing, distance)
}
\arguments{
\item{origin}{a GeoArrow point array}

\item{bearing}{a numeric vector of bearings in degrees; length 1 or the same length as \code{origin}}

\item{distance}{a numeric vector of distances; length 1 or the same length as \code{origin}}
}
\value{
a GeoArrow point array
}
\description{
Returns the point reached by travelling \code{distance} from \code{origin} along
\code{bearing}. Each function uses a different metric space: \code{dest_euclidean}
treats coordinates as planar, \code{dest_haversine} assumes a sphere,
\code{dest_geodesic} an ellipsoid, and \code{dest_rhumb} follows a line of constant
bearing.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# 100km due north and due east of Raleigh
raleigh <- ga_xy(c(-78.6382, -78.6382), c(35.7796, 35.7796))

geoarrow::as_geoarrow_vctr(
  ga_dest_geodesic(raleigh, bearing = c(0, 90), distance = c(1e5, 1e5))
)
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/line_measures/trait.Destination.html}{Destination}
}
\concept{destination}
