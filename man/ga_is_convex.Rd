% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_is_convex}
\alias{ga_is_convex}
\title{Test whether a ring is convex}
\usage{
ga_is_convex(geometry)
}
\arguments{
\item{geometry}{a GeoArrow linestring or polygon array}
}
\value{
a boolean array of the same length as \code{geometry}
}
\description{
\code{TRUE} when the geometry's ring turns consistently in one direction. A
polygon is judged on its exterior ring.
}
\details{
A linestring must be closed, that is form a ring, to be reported convex; an
open one is \code{FALSE}. A polygon's exterior ring is always closed, so polygons
need no special handling. Any other geometry type, or a null geometry,
becomes \code{NA} rather than \code{FALSE}.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
square <- sf::st_polygon(list(
  matrix(c(0, 0, 1, 0, 1, 1, 0, 1, 0, 0), ncol = 2, byrow = TRUE)
))
g <- geoarrow::as_geoarrow_array(sf::st_sfc(square))

as.vector(ga_is_convex(g))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/is_convex/trait.IsConvex.html}{IsConvex}
}
\seealso{
Other query:
\code{\link[=ga_closest_point]{ga_closest_point()}},
\code{\link[=ga_interior_point]{ga_interior_point()}},
\code{\link[=ga_line_locate_point]{ga_line_locate_point()}}
}
\concept{query}
