% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_sparse_knn}
\alias{ga_sparse_knn}
\title{Find the rows of \code{y} nearest each row of \code{x}}
\usage{
ga_sparse_knn(x, y, k = 1, max_distance = NULL)
}
\arguments{
\item{x}{a GeoArrow geometry array}

\item{y}{a GeoArrow geometry array}

\item{k}{how many rows of \code{y} to return per row of \code{x}}

\item{max_distance}{the furthest a match may be, or \code{NULL} for no limit}
}
\value{
a list array of \code{row} and \code{distance} pairs, the same length as \code{x},
where \code{row} is a 1 based row number into \code{y}
}
\description{
Returns the \code{k} rows of \code{y} closest to each row of \code{x}, nearest first, each
paired with the distance between them. This is the sparse form of a nearest
neighbour search, and the shape \code{\link[=ga_knn_join]{ga_knn_join()}} needs.
}
\details{
Distance is Euclidean and measured between the geometries themselves, not
between their bounding boxes, so the nearest edge of a polygon counts rather
than the corner of the box around it. \code{y} is indexed in a packed Hilbert
R-tree, which narrows the search to the rows that can win before any exact
distance is computed, and the answer is the same as comparing every pair.

A row matches fewer than \code{k} rows only when \code{max_distance} rules the rest
out or \code{y} is shorter than \code{k}. A null or empty geometry in \code{x} gives a null
element, and one in \code{y} is never returned.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
nc <- as.data.frame(read_shapefile(
  system.file("shape/nc.shp", package = "sf")
))
sites <- ga_xy(c(-78.6, -80.8), c(35.8, 35.2))

# the three counties nearest each site, with their distances
as.vector(ga_sparse_knn(sites, nc$geometry, k = 3))
\dontshow{\}) # examplesIf}
}
\seealso{
Other index:
\code{\link{KDTree}},
\code{\link{RTree}},
\code{\link[=ga_envelope]{ga_envelope()}},
\code{\link[=ga_knn_join]{ga_knn_join()}}
}
\concept{index}
