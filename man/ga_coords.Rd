% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_coords}
\alias{ga_coords}
\alias{ga_exterior_coords}
\title{Collect a geometry's coordinates as points}
\usage{
ga_coords(geometry)

ga_exterior_coords(geometry)
}
\arguments{
\item{geometry}{a GeoArrow geometry array}
}
\value{
a GeoArrow multipoint array of the same length as \code{geometry}
}
\description{
Returns one multipoint per input geometry, holding that geometry's
vertices in order. The output has the same length as the input.
}
\details{
\code{ga_exterior_coords()} skips the interior rings of a polygon, so it gives the
outline only. Both preserve the order the coordinates are stored in, so a
closed ring repeats its first vertex at the end.

A null geometry stays null.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
ring <- matrix(c(0, 0, 2, 0, 2, 2, 0, 2, 0, 0), ncol = 2, byrow = TRUE)
g <- geoarrow::as_geoarrow_array(sf::st_sfc(sf::st_polygon(list(ring))))

sf::st_as_sfc(geoarrow::as_geoarrow_vctr(ga_coords(g)))
as.vector(nanoarrow::convert_array(ga_n_coords(g)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/coords_iter/trait.CoordsIter.html}{CoordsIter}
}
\seealso{
Other iteration:
\code{\link[=ga_lines]{ga_lines()}},
\code{\link[=ga_n_coords]{ga_n_coords()}}
}
\concept{iteration}
