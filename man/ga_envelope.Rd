% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_envelope}
\alias{ga_envelope}
\title{Compute the bounding box of geometries}
\usage{
ga_envelope(x)
}
\arguments{
\item{x}{a GeoArrow geometry array}
}
\value{
a GeoArrow box array of the same length as \code{x}
}
\description{
Returns the axis aligned bounding box of each geometry, as the box array
that the spatial index queries take.
}
\details{
A box array is passed straight through rather than recomputed, so this is
cheap to call on something that already holds boxes and safe to call
defensively before a query.

This is the envelope in the OGC sense. It differs from
\code{\link[=ga_bounding_rect]{ga_bounding_rect()}} only in that pass through; the boxes themselves are
the same.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
nc <- as.data.frame(read_shapefile(
  system.file("shape/nc.shp", package = "sf")
))
boxes <- ga_envelope(nc$geometry)
head(geoarrow::as_geoarrow_vctr(boxes), 3)

# already boxes, so this is a no op
identical(
  as.character(geoarrow::as_geoarrow_vctr(ga_envelope(boxes))),
  as.character(geoarrow::as_geoarrow_vctr(boxes))
)
\dontshow{\}) # examplesIf}
}
\seealso{
Other index:
\code{\link{KDTree}},
\code{\link{RTree}},
\code{\link[=ga_knn_join]{ga_knn_join()}},
\code{\link[=ga_set_thread_pool]{ga_set_thread_pool()}},
\code{\link[=ga_sparse_dwithin]{ga_sparse_dwithin()}},
\code{\link[=ga_sparse_knn]{ga_sparse_knn()}}
}
\concept{index}
