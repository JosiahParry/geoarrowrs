% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_is_valid}
\alias{ga_is_valid}
\alias{ga_validation_error}
\title{Test whether geometries are well formed}
\usage{
ga_is_valid(geometry)

ga_validation_error(geometry)
}
\arguments{
\item{geometry}{a GeoArrow geometry array}
}
\value{
a boolean array of the same length as \code{geometry}

a string array of the same length as \code{geometry}
}
\description{
\code{TRUE} when a geometry satisfies the OGC simple features rules.

Returns the first validation problem found, or \code{NA} when the geometry is
valid.
}
\details{
Several algorithms give wrong answers on invalid input rather than failing,
so this is worth checking before trusting a result. Common problems are a
polygon whose rings self intersect, a ring with too few points, and a
coordinate that is \code{NaN} or infinite.

A null geometry gives \code{NA}. Use \code{\link[=ga_validation_error]{ga_validation_error()}} to see why a geometry
failed.

A geometry can break more than one rule; only the first is reported, since
fixing it often resolves the rest.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
good <- sf::st_polygon(list(matrix(
  c(0, 0, 2, 0, 2, 2, 0, 2, 0, 0), ncol = 2, byrow = TRUE
)))
bowtie <- sf::st_polygon(list(matrix(
  c(0, 0, 2, 2, 2, 0, 0, 2, 0, 0), ncol = 2, byrow = TRUE
)))
g <- geoarrow::as_geoarrow_array(sf::st_sfc(good, bowtie))

as.vector(ga_is_valid(g))
as.vector(ga_validation_error(g))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/validation/trait.Validation.html}{Validation}

\href{https://docs.rs/geo/latest/geo/algorithm/validation/trait.Validation.html}{Validation}
}
\concept{validation}
