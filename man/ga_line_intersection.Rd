% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_line_intersection}
\alias{ga_line_intersection}
\title{Intersect pairs of two point lines}
\usage{
ga_line_intersection(x, y)
}
\arguments{
\item{x}{a GeoArrow array of two point linestrings}

\item{y}{a GeoArrow array of two point linestrings; length 1 or the same
length as \code{x}}
}
\value{
a GeoArrow multipoint array of the same length as \code{x}
}
\description{
Returns where each pair of lines meets: a point when they cross, and a line
when they overlap along a shared stretch. \code{y} is recycled against \code{x}.
}
\details{
The answer is a point when the lines cross and a segment when they overlap.
Both come back as a multipoint so the column has one type: a crossing gives
one point, an overlap gives the two endpoints of the shared stretch. So
\code{ga_n_coords()} tells the two apart, and an overlap can be rebuilt from its
endpoints.

Both arguments must hold single segments, that is a \code{LINE} or a two point
\code{LINESTRING}. Longer linestrings, other geometry types, and null rows come
back null, as do pairs that simply do not meet. Use
\code{\link[=ga_self_intersections]{ga_self_intersections()}} for a geometry with many segments.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
a <- sf::st_linestring(cbind(c(0, 2), c(0, 2)))
b <- sf::st_linestring(cbind(c(0, 2), c(2, 0)))
x <- geoarrow::as_geoarrow_array(sf::st_sfc(a))
y <- geoarrow::as_geoarrow_array(sf::st_sfc(b))

sf::st_as_sfc(geoarrow::as_geoarrow_vctr(ga_line_intersection(x, y)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/line_intersection/fn.line_intersection.html}{line_intersection}
}
\seealso{
Other intersection:
\code{\link[=ga_self_intersections]{ga_self_intersections()}}
}
\concept{intersection}
