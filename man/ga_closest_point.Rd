% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_closest_point}
\alias{ga_closest_point}
\alias{ga_closest_point_haversine}
\title{Find the point on a geometry closest to another point}
\usage{
ga_closest_point(geometry, point)

ga_closest_point_haversine(geometry, point)
}
\arguments{
\item{geometry}{a GeoArrow geometry array}

\item{point}{a GeoArrow point array; length 1 or the same length as \code{geometry}}
}
\value{
a GeoArrow point array of the same length as \code{geometry}
}
\description{
Returns the position on each geometry nearest the corresponding point,
using planar distance. \code{ga_closest_point_haversine()} measures on a sphere
instead, treating coordinates as longitude and latitude in degrees.
}
\details{
When the point lies on the geometry the intersection itself is returned.
A geometry with no single nearest position, such as a point equidistant
from both ends of a symmetric line, becomes a null element, as does a null
geometry or a null point.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
line <- geoarrow::as_geoarrow_array(sf::st_sfc(
  sf::st_linestring(cbind(c(0, 10), c(0, 0)))
))
pt <- geoarrow::as_geoarrow_array(sf::st_sfc(sf::st_point(c(4, 5))))

sf::st_as_sfc(geoarrow::as_geoarrow_vctr(ga_closest_point(line, pt)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/closest_point/trait.ClosestPoint.html}{ClosestPoint}

\href{https://docs.rs/geo/latest/geo/algorithm/haversine_closest_point/trait.HaversineClosestPoint.html}{HaversineClosestPoint}
}
\seealso{
Other query:
\code{\link[=ga_interior_point]{ga_interior_point()}},
\code{\link[=ga_is_convex]{ga_is_convex()}},
\code{\link[=ga_line_locate_point]{ga_line_locate_point()}}
}
\concept{query}
