% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_sparse_intersects}
\alias{ga_sparse_intersects}
\alias{ga_sparse_contains}
\alias{ga_sparse_contains_properly}
\alias{ga_sparse_within}
\alias{ga_sparse_covers}
\alias{ga_sparse_covered_by}
\alias{ga_sparse_touches}
\alias{ga_sparse_crosses}
\alias{ga_sparse_overlaps}
\alias{ga_sparse_equals_topo}
\title{Find which rows of \code{y} relate to each row of \code{x}}
\usage{
ga_sparse_intersects(x, y)

ga_sparse_contains(x, y)

ga_sparse_contains_properly(x, y)

ga_sparse_within(x, y)

ga_sparse_covers(x, y)

ga_sparse_covered_by(x, y)

ga_sparse_touches(x, y)

ga_sparse_crosses(x, y)

ga_sparse_overlaps(x, y)

ga_sparse_equals_topo(x, y)
}
\arguments{
\item{x}{a GeoArrow geometry array}

\item{y}{a GeoArrow geometry array}
}
\value{
a list array of 1 based row numbers into \code{y}, the same length as \code{x}
}
\description{
Each function compares every row of \code{x} against every row of \code{y} and returns
the row numbers of \code{y} for which the named DE-9IM relationship holds. This is
the sparse form of the pairwise predicates in \code{\link[=ga_intersects]{ga_intersects()}}, and the
shape \code{\link[=ga_join]{ga_join()}} needs.
}
\details{
The comparison is not the full cross product. \code{y} is indexed in a packed
Hilbert R-tree and only the rows whose bounding box overlaps are relate
tested, so the cost scales with the number of candidates rather than with
\code{length(x) * length(y)}.

A row of \code{x} that matches nothing gives a zero length element, not a null. A
null or empty geometry in \code{x} gives a null element, and one in \code{y} is never
returned.

\code{\link[=ga_disjoint]{ga_disjoint()}} has no sparse form. Disjointness is the one relationship a
bounding box cannot narrow, so the answer is almost every row of \code{y} and the
result is denser than the input.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
nc <- as.data.frame(read_shapefile(
  system.file("shape/nc.shp", package = "sf")
))

# which counties each county touches
nbrs <- as.vector(ga_sparse_touches(nc$geometry, nc$geometry))
nbrs[[1]]

# how many neighbours each has
summary(lengths(nbrs))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/relate/trait.Relate.html}{Relate}
}
\seealso{
Other topology:
\code{\link[=ga_contains]{ga_contains()}},
\code{\link[=ga_coordinate_position]{ga_coordinate_position()}},
\code{\link[=ga_dimension]{ga_dimension()}},
\code{\link[=ga_filter]{ga_filter()}},
\code{\link[=ga_is_empty]{ga_is_empty()}},
\code{\link[=ga_join]{ga_join()}},
\code{\link[=ga_relate]{ga_relate()}},
\code{\link[=ga_sparse_pairs]{ga_sparse_pairs()}}
}
\concept{topology}
