% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{KDTree}
\alias{KDTree}
\alias{$.KDTree}
\title{A k-d tree over a point array}
\usage{
KDTree
}
\value{
a \code{KDTree} object
}
\description{
Finds which points sit in a box, or near another point.
}
\section{Methods}{

\subsection{Method \code{new}}{
Build the index. A bigger \code{node_size} builds quicker but searches slower.
}

\subsection{Method \code{range}}{
Which points fall inside each geometry's box

\subsection{Arguments}{
\describe{
\item{\code{geometry}}{a GeoArrow array to look up}
}}
\subsection{returns}{
a list array of 1 based row numbers, the same length as
\code{geometry}
}
}

\subsection{Method \code{within}}{
Which points are within \code{r} of each point, measured flat in coordinate units

\subsection{Arguments}{
\describe{
\item{\code{geometry}}{a GeoArrow point array to look up}
\item{\code{r}}{the radius; length 1 or the same length as \code{geometry}}
}}
\subsection{returns}{
a list array of 1 based row numbers, the same length as
\code{geometry}
}
}

\subsection{Method \code{size}}{
How many rows went in

\subsection{returns}{
the length of the array the index was built from
}
}

\subsection{Method \code{n_indexed}}{
How many rows made it into the tree, skipping null and empty points

\subsection{returns}{
the number of indexed rows
}
}
}

\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fp <- system.file("shape/nc.shp", package = "sf")
nc <- as.data.frame(read_shapefile(fp))
pts <- ga_centroid(nc$geometry)
idx <- KDTree$new(pts)

# every centroid within half a degree of every other
head(as.vector(idx$within(pts, 0.5)), 3)

# a single lookup is an array of one
as.vector(idx$within(ga_xy(-78.6, 35.8), 0.5))[[1]]
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo-index/latest/geo_index/kdtree/index.html}{geo-index}
}
\seealso{
Other index:
\code{\link{RTree}},
\code{\link[=ga_envelope]{ga_envelope()}},
\code{\link[=ga_knn_join]{ga_knn_join()}},
\code{\link[=ga_set_thread_pool]{ga_set_thread_pool()}},
\code{\link[=ga_sparse_dwithin]{ga_sparse_dwithin()}},
\code{\link[=ga_sparse_knn]{ga_sparse_knn()}}
}
\concept{index}
