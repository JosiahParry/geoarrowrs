% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_make_line}
\alias{ga_make_line}
\title{Build a line between pairs of points}
\usage{
ga_make_line(start, end)
}
\arguments{
\item{start}{a GeoArrow point array}

\item{end}{a GeoArrow point array; length 1 or the same length as \code{start}}
}
\value{
a GeoArrow linestring array of the same length as \code{start}
}
\description{
Returns a two point linestring joining each \code{start} to the matching \code{end}.
\code{end} is recycled, so one destination pairs with every origin.
}
\details{
A null or empty point on either side gives a null element, so the result is
always the same length as \code{start}. The length of the line is
\code{\link[=ga_length_euclidean]{ga_length_euclidean()}}, which equals \code{\link[=ga_dist_euclidean_pairwise]{ga_dist_euclidean_pairwise()}} on the
same pair.
}
\examples{
\dontshow{if (requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
start <- ga_xy(c(0, 0), c(0, 3))
end <- ga_xy(c(4, 4), c(0, 3))

as.vector(ga_length_euclidean(ga_make_line(start, end)))
\dontshow{\}) # examplesIf}
}
\seealso{
Other construct:
\code{\link[=ga_xy]{ga_xy()}}
}
\concept{construct}
