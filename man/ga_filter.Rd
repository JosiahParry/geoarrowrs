% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{ga_filter}
\alias{ga_filter}
\title{Keep the rows of a data frame that relate to another spatially}
\usage{
ga_filter(x, y, predicate = ga_sparse_intersects, ...)
}
\arguments{
\item{x, y}{data frames, each with one GeoArrow geometry column}

\item{predicate}{a sparse predicate, by default \code{\link[=ga_sparse_intersects]{ga_sparse_intersects()}}}
}
\value{
an Arrow table holding the rows of \code{x} that matched
}
\description{
Filters \code{x} down to the rows whose geometry relates to any row of \code{y}. The
columns of \code{y} are not attached, which is the difference from \code{\link[=ga_join]{ga_join()}}.
}
\details{
A row of \code{x} is kept when the predicate finds it at least one match in \code{y},
so \code{ga_filter(sites, counties, ga_sparse_within)} keeps the sites that fall
in any county. This is \code{ST_Filter()}, and the same answer as
\code{ga_join(x, y, predicate, left = FALSE)} with the columns of \code{y} dropped and
the rows of \code{x} not repeated.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
nc <- as.data.frame(read_shapefile(
  system.file("shape/nc.shp", package = "sf")
))
sites <- data.frame(
  site = c("a", "b", "c"),
  geometry = geoarrow::as_geoarrow_vctr(
    ga_xy(c(-78.6, -80.8, 0), c(35.8, 35.2, 0))
  )
)

# the third site is in the Atlantic, so it goes
ga_filter(sites, nc, ga_sparse_within)
\dontshow{\}) # examplesIf}
}
\seealso{
Other topology:
\code{\link[=ga_contains]{ga_contains()}},
\code{\link[=ga_coordinate_position]{ga_coordinate_position()}},
\code{\link[=ga_dimension]{ga_dimension()}},
\code{\link[=ga_is_empty]{ga_is_empty()}},
\code{\link[=ga_join]{ga_join()}},
\code{\link[=ga_relate]{ga_relate()}},
\code{\link[=ga_sparse_intersects]{ga_sparse_intersects()}},
\code{\link[=ga_sparse_pairs]{ga_sparse_pairs()}}
}
\concept{topology}
