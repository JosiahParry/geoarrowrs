% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_x}
\alias{ga_x}
\alias{ga_y}
\title{Read the x or y of a point}
\usage{
ga_x(geometry)

ga_y(geometry)
}
\arguments{
\item{geometry}{a GeoArrow point array}
}
\value{
a double array of the same length as \code{geometry}
}
\description{
Returns one coordinate per row. Any geometry that is not a single point, and
any null or empty point, gives \code{NA}.
}
\details{
Both coordinate encodings work, so a point array stored as a struct of two
double columns and one stored as interleaved values read the same.

Use \code{\link[=ga_coords]{ga_coords()}} for the vertices of a line or polygon, which returns a
multipoint per row rather than a single number.
}
\examples{
\dontshow{if (requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
pts <- ga_xy(c(-111.76, -112.07), c(34.87, 33.45))

as.vector(ga_x(pts))
as.vector(ga_y(pts))
\dontshow{\}) # examplesIf}
}
\seealso{
Other iteration:
\code{\link[=ga_coords]{ga_coords()}},
\code{\link[=ga_lines]{ga_lines()}},
\code{\link[=ga_n_coords]{ga_n_coords()}}
}
\concept{iteration}
