% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_sparse_dwithin}
\alias{ga_sparse_dwithin}
\title{Find which rows of \code{y} lie within a distance of each row of \code{x}}
\usage{
ga_sparse_dwithin(x, y, distance)
}
\arguments{
\item{x}{a GeoArrow geometry array}

\item{y}{a GeoArrow geometry array}

\item{distance}{the furthest a row of \code{y} may be; length 1 or the same
length as \code{x}}
}
\value{
a list array of 1 based row numbers into \code{y}, the same length as \code{x}
}
\description{
Returns the row numbers of \code{y} whose geometry is no further than \code{distance}
from each row of \code{x}. This is the sparse form of a distance band join, and
the same test as PostGIS \code{ST_DWithin()}.
}
\details{
Distance is Euclidean and measured between the geometries themselves, so a
point counts when it is within \code{distance} of the nearest edge of a polygon,
not of the box around it. This is why it differs from buffering \code{x} and
intersecting: a buffer approximates its curves with segments, so a point
just inside the true radius can fall outside the buffer.

The bounding box of each row of \code{x} is grown by \code{distance} before the tree
is searched, so nothing within reach is missed and only the rows that could
qualify are measured. \code{distance} is recycled, so one value covers every row
or a different radius can apply to each.

A row that matches nothing gives a zero length element, not a null. A null
or empty geometry in \code{x}, or a null \code{distance}, gives a null element, and a
null geometry in \code{y} is never returned.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
nc <- as.data.frame(read_shapefile(
  system.file("shape/nc.shp", package = "sf")
))
sites <- ga_xy(c(-78.6, -80.8), c(35.8, 35.2))

# the counties within a quarter degree of each site
as.vector(ga_sparse_dwithin(sites, nc$geometry, 0.25))
\dontshow{\}) # examplesIf}
}
\seealso{
Other index:
\code{\link{KDTree}},
\code{\link{RTree}},
\code{\link[=ga_envelope]{ga_envelope()}},
\code{\link[=ga_knn_join]{ga_knn_join()}},
\code{\link[=ga_sparse_knn]{ga_sparse_knn()}}
}
\concept{index}
