% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_contains}
\alias{ga_contains}
\alias{ga_contains_properly}
\alias{ga_within}
\alias{ga_covers}
\alias{ga_covered_by}
\alias{ga_intersects}
\alias{ga_disjoint}
\alias{ga_touches}
\alias{ga_crosses}
\alias{ga_overlaps}
\alias{ga_equals_topo}
\title{Test a topological relationship between two geometry arrays}
\usage{
ga_contains(x, y)

ga_contains_properly(x, y)

ga_within(x, y)

ga_covers(x, y)

ga_covered_by(x, y)

ga_intersects(x, y)

ga_disjoint(x, y)

ga_touches(x, y)

ga_crosses(x, y)

ga_overlaps(x, y)

ga_equals_topo(x, y)
}
\arguments{
\item{x}{a GeoArrow geometry array}

\item{y}{a GeoArrow geometry array; length 1 or the same length as \code{x}}
}
\value{
a boolean array of the same length as \code{x}
}
\description{
Each function compares \code{x} and \code{y} row by row and returns \code{TRUE} when the
named DE-9IM relationship holds. \code{y} is recycled against \code{x}.
}
\details{
\code{ga_contains()} is \code{TRUE} when no point of \code{y} lies outside \code{x} and at least
one point of \code{y} lies in the interior of \code{x}. \code{ga_within()} is the same test
with the arguments swapped. \code{ga_covers()} and \code{ga_covered_by()} are the weaker
forms that allow every shared point to lie on the boundary.
\code{ga_contains_properly()} is the stricter form requiring \code{y} to fall entirely
within the interior.

\code{ga_intersects()} and \code{ga_disjoint()} are negations of one another.
\code{ga_touches()} is \code{TRUE} when the geometries share a boundary point but no
interior point, \code{ga_crosses()} when their interiors meet in a lower dimension
than at least one of them, and \code{ga_overlaps()} when they meet in the same
dimension as both. \code{ga_equals_topo()} compares point sets rather than
coordinate order, so two geometries wound differently are still equal.

A null geometry on either side gives \code{NA}.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
big <- sf::st_polygon(list(
  matrix(c(0, 0, 4, 0, 4, 4, 0, 4, 0, 0), ncol = 2, byrow = TRUE)
))
x <- geoarrow::as_geoarrow_array(sf::st_sfc(big))
y <- geoarrow::as_geoarrow_array(sf::st_sfc(sf::st_point(c(2, 2))))

as.vector(nanoarrow::convert_array(ga_contains(x, y)))
as.vector(nanoarrow::convert_array(ga_intersects(x, y)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/relate/trait.Relate.html}{Relate}
}
\seealso{
Other topology:
\code{\link[=ga_coordinate_position]{ga_coordinate_position()}},
\code{\link[=ga_dimension]{ga_dimension()}},
\code{\link[=ga_is_empty]{ga_is_empty()}},
\code{\link[=ga_relate]{ga_relate()}}
}
\concept{topology}
