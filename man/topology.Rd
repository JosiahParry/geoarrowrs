% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{contains}
\alias{contains}
\alias{contains_properly}
\alias{within}
\alias{covers}
\alias{covered_by}
\alias{intersects}
\alias{disjoint}
\alias{touches}
\alias{crosses}
\alias{overlaps}
\alias{equals_topo}
\title{Test a topological relationship between two geometry arrays}
\usage{
contains(x, y)

contains_properly(x, y)

within(x, y)

covers(x, y)

covered_by(x, y)

intersects(x, y)

disjoint(x, y)

touches(x, y)

crosses(x, y)

overlaps(x, y)

equals_topo(x, y)
}
\arguments{
\item{x}{a GeoArrow geometry array}

\item{y}{a GeoArrow geometry array; length 1 or the same length as \code{x}}
}
\value{
a boolean array of the same length as \code{x}
}
\description{
Each function compares \code{x} and \code{y} row by row and returns \code{TRUE} when the
named DE-9IM relationship holds. \code{y} is recycled against \code{x}.
}
\details{
\code{contains()} is \code{TRUE} when no point of \code{y} lies outside \code{x} and at least
one point of \code{y} lies in the interior of \code{x}. \code{within()} is the same test
with the arguments swapped. \code{covers()} and \code{covered_by()} are the weaker
forms that allow every shared point to lie on the boundary.
\code{contains_properly()} is the stricter form requiring \code{y} to fall entirely
within the interior.

\code{intersects()} and \code{disjoint()} are negations of one another.
\code{touches()} is \code{TRUE} when the geometries share a boundary point but no
interior point, \code{crosses()} when their interiors meet in a lower dimension
than at least one of them, and \code{overlaps()} when they meet in the same
dimension as both. \code{equals_topo()} compares point sets rather than
coordinate order, so two geometries wound differently are still equal.

A null geometry on either side gives \code{NA}.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
big <- sf::st_polygon(list(
  matrix(c(0, 0, 4, 0, 4, 4, 0, 4, 0, 0), ncol = 2, byrow = TRUE)
))
x <- geoarrow::as_geoarrow_array(sf::st_sfc(big))
y <- geoarrow::as_geoarrow_array(sf::st_sfc(sf::st_point(c(2, 2))))

as.vector(nanoarrow::convert_array(contains(x, y)))
as.vector(nanoarrow::convert_array(intersects(x, y)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/relate/trait.Relate.html}{Relate}
}
\seealso{
Other topology:
\code{\link[=coordinate_position]{coordinate_position()}},
\code{\link[=dimension]{dimension()}},
\code{\link[=is_empty]{is_empty()}},
\code{\link[=relate]{relate()}}
}
\concept{topology}
