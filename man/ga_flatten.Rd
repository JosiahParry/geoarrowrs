% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_flatten}
\alias{ga_flatten}
\title{Collapse a list of geometries into a single array}
\usage{
ga_flatten(x)
}
\arguments{
\item{x}{a list array of GeoArrow arrays, as returned by \code{\link[=ga_explode]{ga_explode()}}}
}
\value{
a GeoArrow array holding every part, flattened
}
\description{
The inverse of \code{\link[=ga_explode]{ga_explode()}}. Concatenates every element's parts into one
array, so a list of \code{n} elements holding \code{m} parts in total becomes an
array of length \code{m}.
}
\details{
Null elements contribute nothing, so the result is shorter than the input
whenever an element holds more or fewer than one geometry.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fp <- system.file("shape/nc.shp", package = "sf")
nc <- as.data.frame(read_shapefile(fp))

# 100 counties in, one row per polygon part out
length(nc$geometry)
ga_flatten(ga_explode(nc$geometry))$length
\dontshow{\}) # examplesIf}
}
\seealso{
Other cast:
\code{\link[=ga_as_point]{ga_as_point()}},
\code{\link[=ga_cast_geometry]{ga_cast_geometry()}},
\code{\link[=ga_downcast_geometry]{ga_downcast_geometry()}},
\code{\link[=ga_explode]{ga_explode()}},
\code{\link[=ga_from_wkb]{ga_from_wkb()}}
}
\concept{cast}
