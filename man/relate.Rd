% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{relate}
\alias{relate}
\title{Compute the DE-9IM relationship between two geometry arrays}
\usage{
relate(x, y)
}
\arguments{
\item{x}{a GeoArrow geometry array}

\item{y}{a GeoArrow geometry array; length 1 or the same length as \code{x}}
}
\value{
a string array of the same length as \code{x}
}
\description{
Returns the nine character DE-9IM matrix describing how each pair of
geometries relates, from which every named predicate can be derived.
}
\details{
The string reads as the intersections of the interior, boundary and
exterior of \code{x} with those of \code{y}, in that order. Each character is the
dimension of that intersection: \code{F} for empty, \code{0} for a point, \code{1} for a
curve, and \code{2} for a surface. A null geometry on either side gives \code{NA}.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
big <- sf::st_polygon(list(
  matrix(c(0, 0, 4, 0, 4, 4, 0, 4, 0, 0), ncol = 2, byrow = TRUE)
))
x <- geoarrow::as_geoarrow_array(sf::st_sfc(big))
y <- geoarrow::as_geoarrow_array(sf::st_sfc(sf::st_point(c(2, 2))))

as.vector(nanoarrow::convert_array(relate(x, y)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/relate/trait.Relate.html}{Relate}
}
\seealso{
Other topology:
\code{\link[=contains]{contains()}},
\code{\link[=coordinate_position]{coordinate_position()}},
\code{\link[=dimension]{dimension()}},
\code{\link[=is_empty]{is_empty()}}
}
\concept{topology}
