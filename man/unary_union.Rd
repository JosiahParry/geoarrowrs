% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{unary_union}
\alias{unary_union}
\title{Dissolve an entire array of polygons into one}
\usage{
unary_union(x)
}
\arguments{
\item{x}{a GeoArrow polygon or multipolygon array}
}
\value{
a GeoArrow multipolygon array of length 1
}
\description{
Merges every polygon in \code{x} into a single multipolygon, dropping the
boundaries between any that touch or overlap.
}
\details{
This is the one function in the package that is not length-preserving. It
is an aggregate over the whole array, so it always returns a length 1
array, in the way \code{sum()} reduces a vector to a single value.

It is far faster than folding \code{\link[=boolean_union]{boolean_union()}} across the array, since it
unions all the rings in one pass. Rows that are not polygonal, and null
rows, are skipped.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
bx <- function(a, b, c, d) sf::st_polygon(list(matrix(
  c(a, b, c, b, c, d, a, d, a, b), ncol = 2, byrow = TRUE
)))
x <- geoarrow::as_geoarrow_array(sf::st_sfc(bx(0, 0, 2, 2), bx(1, 1, 3, 3)))

sf::st_area(sf::st_as_sfc(geoarrow::as_geoarrow_vctr(unary_union(x))))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/bool_ops/fn.unary_union.html}{unary_union}
}
\seealso{
Other boolean:
\code{\link[=boolean_intersection]{boolean_intersection()}}
}
\concept{boolean}
