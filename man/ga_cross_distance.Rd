% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_cross_distance}
\alias{ga_cross_distance}
\title{Distance from every row of \code{x} to every row of \code{y}}
\usage{
ga_cross_distance(x, y, metric = "euclidean")
}
\arguments{
\item{x}{a GeoArrow geometry array for \code{metric = "euclidean"}, a point array
for the others}

\item{y}{a GeoArrow array matching \code{x}}

\item{metric}{one of \code{"euclidean"}, \code{"haversine"}, \code{"geodesic"}, \code{"rhumb"},
or \code{"vincenty"}}
}
\value{
a list array with one element per row of \code{x}, each holding
\code{length(y)} distances
}
\description{
Measures the full cross product rather than walking the two arrays in
lockstep, giving one list of \code{length(y)} distances per row of \code{x}.
}
\details{
This is the shape \code{\link[=ga_dist_euclidean_pairwise]{ga_dist_euclidean_pairwise()}} cannot express, and it
costs \code{length(x) * length(y)} distances to hold: ten thousand rows against
ten thousand is a hundred million doubles, or eight hundred megabytes. Where
the distances are only wanted to pick a nearest row or a threshold,
\code{\link[=ga_sparse_knn]{ga_sparse_knn()}} and \code{\link[=ga_sparse_dwithin]{ga_sparse_dwithin()}} answer that against an R-tree
without ever forming the product.

\code{"euclidean"} measures between geometries of any type. The spherical and
ellipsoidal metrics take points only, because \code{geo} defines them between
points alone. \code{"vincenty"} gives a null where the algorithm fails to
converge.

A null row of \code{x} gives a null element. A null row of \code{y} gives a null in
that position of every element.
}
\examples{
\dontshow{if (requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
x <- ga_xy(c(-78.6382, -80.8431), c(35.7796, 35.2271))
y <- ga_xy(c(-77.9447, -78.6382, -80.8431), c(34.2257, 35.7796, 35.2271))

# two elements of three distances each, in meters
ga_cross_distance(x, y, "haversine")
\dontshow{\}) # examplesIf}
}
\seealso{
Other distance:
\code{\link[=ga_dist_euclidean_pairwise]{ga_dist_euclidean_pairwise()}},
\code{\link[=ga_dist_frechet_pairwise]{ga_dist_frechet_pairwise()}},
\code{\link[=ga_dist_hausdorff_pairwise]{ga_dist_hausdorff_pairwise()}},
\code{\link[=ga_dist_vincenty_pairwise]{ga_dist_vincenty_pairwise()}}
}
\concept{distance}
