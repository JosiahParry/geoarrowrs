% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{affine_transform}
\alias{affine_transform}
\title{Apply an arbitrary affine transform}
\usage{
affine_transform(geometry, a, b, xoff, d, e, yoff)
}
\arguments{
\item{geometry}{a GeoArrow point, linestring, multilinestring, polygon, or
multipolygon array}

\item{a, b, d, e}{the linear part of the matrix; each length 1 or the same
length as \code{geometry}}

\item{xoff, yoff}{the translation part; each length 1 or the same length as
\code{geometry}}
}
\value{
a GeoArrow array of the same geometry type as \code{geometry}
}
\description{
Transforms every coordinate by the six coefficients of an affine matrix.
The geometry type of the output matches the input.
}
\details{
The coefficients map a coordinate to
\verb{x' = a * x + b * y + xoff} and \verb{y' = d * x + e * y + yoff}, the same
ordering PostGIS \code{ST_Affine} and Shapely use. The identity transform is
\verb{a = 1, b = 0, xoff = 0, d = 0, e = 1, yoff = 0}.

This is the general form behind \code{\link[=translate]{translate()}}, \code{\link[=scale_xy]{scale_xy()}}, \code{\link[=rotate_around_centroid]{rotate_around_centroid()}},
and \code{\link[=skew]{skew()}}. Reach for those when they fit, since they are clearer at the
call site. Use this one to apply a matrix you already have, or to compose
several steps into a single pass over the coordinates.

All six coefficients are recycled against \code{geometry}, so a different
transform can be applied to every row.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
square <- sf::st_polygon(list(matrix(
  c(0, 0, 1, 0, 1, 1, 0, 1, 0, 0), ncol = 2, byrow = TRUE
)))
g <- geoarrow::as_geoarrow_array(sf::st_sfc(square))

# double the width and shift right by 10
res <- affine_transform(g, a = 2, b = 0, xoff = 10, d = 0, e = 1, yoff = 0)
sf::st_as_sfc(geoarrow::as_geoarrow_vctr(res))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/affine_ops/trait.AffineOps.html}{AffineOps}
}
\seealso{
Other affine:
\code{\link[=rotate_around_center]{rotate_around_center()}},
\code{\link[=rotate_around_centroid]{rotate_around_centroid()}},
\code{\link[=scale_xy]{scale_xy()}},
\code{\link[=skew]{skew()}},
\code{\link[=skew_xy]{skew_xy()}},
\code{\link[=translate]{translate()}}
}
\concept{affine}
