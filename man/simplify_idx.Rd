% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{simplify_idx}
\alias{simplify_idx}
\alias{simplify_vw_idx}
\title{Find which coordinates simplification would keep}
\usage{
simplify_idx(geometry, epsilon)

simplify_vw_idx(geometry, epsilon)
}
\arguments{
\item{geometry}{a GeoArrow linestring array}

\item{epsilon}{the simplification tolerance; length 1 or the same length as
\code{geometry}}
}
\value{
a list array of integer positions, one list per input geometry
}
\description{
Returns the positions of the coordinates that survive simplification,
rather than the simplified geometry itself. One list per input geometry.
}
\details{
Use these when the coordinates carry data of their own, such as a timestamp
or a sensor reading per vertex. Simplifying the geometry discards that
alignment; the indices let you subset the other columns the same way.

\code{simplify_idx()} uses Ramer-Douglas-Peucker and \code{simplify_vw_idx()} uses
Visvalingam-Whyatt, matching \code{\link[=simplify]{simplify()}} and \code{\link[=simplify_vw]{simplify_vw()}}. Indices are
1 based and always include the first and last coordinate.

Only linestrings can be simplified this way. Any other geometry type, or a
null geometry, comes back null.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
line <- sf::st_linestring(cbind(c(0, 1, 2, 3, 4), c(0, 0.1, 0, 0.1, 0)))
g <- geoarrow::as_geoarrow_array(sf::st_sfc(line))

nanoarrow::convert_array(simplify_idx(g, 0.5))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/simplify/trait.SimplifyIdx.html}{SimplifyIdx}

\href{https://docs.rs/geo/latest/geo/algorithm/simplify_vw/trait.SimplifyVwIdx.html}{SimplifyVwIdx}
}
\seealso{
Other simplify:
\code{\link[=simplify]{simplify()}},
\code{\link[=simplify_vw]{simplify_vw()}}
}
\concept{simplify}
