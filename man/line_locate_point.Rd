% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{line_locate_point}
\alias{line_locate_point}
\title{Locate a point along a line as a fraction of its length}
\usage{
line_locate_point(geometry, point)
}
\arguments{
\item{geometry}{a GeoArrow linestring array}

\item{point}{a GeoArrow point array; length 1 or the same length as \code{geometry}}
}
\value{
a double array of the same length as \code{geometry}
}
\description{
Returns how far along each line the closest position to the corresponding
point lies, as a fraction between 0 and 1.
}
\details{
0 is the start of the line and 1 its end, so the value multiplied by the
line's length gives a distance. Only lines and linestrings can be located
along; any other geometry type, a null geometry, a null point, or a
zero length line becomes \code{NA}.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
line <- geoarrow::as_geoarrow_array(sf::st_sfc(
  sf::st_linestring(cbind(c(0, 10), c(0, 0)))
))
pt <- geoarrow::as_geoarrow_array(sf::st_sfc(sf::st_point(c(2.5, 0))))

as.vector(nanoarrow::convert_array(line_locate_point(line, pt)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/line_locate_point/trait.LineLocatePoint.html}{LineLocatePoint}
}
\seealso{
Other query:
\code{\link[=closest_point]{closest_point()}},
\code{\link[=interior_point]{interior_point()}},
\code{\link[=is_convex]{is_convex()}}
}
\concept{query}
