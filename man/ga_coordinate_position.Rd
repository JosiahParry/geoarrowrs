% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_coordinate_position}
\alias{ga_coordinate_position}
\title{Locate a point relative to a geometry}
\usage{
ga_coordinate_position(geometry, point)
}
\arguments{
\item{geometry}{a GeoArrow geometry array}

\item{point}{a GeoArrow point array; length 1 or the same length as \code{geometry}}
}
\value{
a string array of the same length as \code{geometry}
}
\description{
Returns \code{"inside"}, \code{"outside"}, or \code{"boundary"} for each pair.
}
\details{
This is the three way form of a point in polygon test, distinguishing a
point that lies exactly on an edge from one strictly inside. \code{point} is
recycled against \code{geometry}. A null geometry or a null point gives \code{NA}.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
square <- sf::st_polygon(list(
  matrix(c(0, 0, 4, 0, 4, 4, 0, 4, 0, 0), ncol = 2, byrow = TRUE)
))
g <- geoarrow::as_geoarrow_array(sf::st_sfc(square, square, square))
p <- geoarrow::as_geoarrow_array(sf::st_sfc(
  sf::st_point(c(2, 2)), sf::st_point(c(0, 2)), sf::st_point(c(9, 9))
))

as.vector(ga_coordinate_position(g, p))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/coordinate_position/trait.CoordinatePosition.html}{CoordinatePosition}
}
\seealso{
Other topology:
\code{\link[=ga_contains]{ga_contains()}},
\code{\link[=ga_dimension]{ga_dimension()}},
\code{\link[=ga_is_empty]{ga_is_empty()}},
\code{\link[=ga_relate]{ga_relate()}}
}
\concept{topology}
