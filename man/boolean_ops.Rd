% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{boolean_intersection}
\alias{boolean_intersection}
\alias{boolean_union}
\alias{boolean_difference}
\alias{boolean_xor}
\title{Combine two polygon arrays with a set operation}
\usage{
boolean_intersection(x, y)

boolean_union(x, y)

boolean_difference(x, y)

boolean_xor(x, y)
}
\arguments{
\item{x}{a GeoArrow polygon or multipolygon array}

\item{y}{a GeoArrow polygon or multipolygon array; length 1 or the same
length as \code{x}}
}
\value{
a GeoArrow multipolygon array of the same length as \code{x}
}
\description{
Each function pairs \code{x} with \code{y} row by row and returns the polygonal
result. \code{y} is recycled against \code{x}.
}
\details{
\code{boolean_intersection()} keeps the area in both, \code{boolean_union()} the area
in either, \code{boolean_difference()} the area in \code{x} but not \code{y}, and
\code{boolean_xor()} the area in exactly one of them.

These are defined for polygons and multipolygons only. A row whose geometry
is any other type, or is null, comes back null. The result is always a
multipolygon, since a set operation can split one polygon into several or
erase it entirely.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
bx <- function(a, b, c, d) sf::st_polygon(list(matrix(
  c(a, b, c, b, c, d, a, d, a, b), ncol = 2, byrow = TRUE
)))
x <- geoarrow::as_geoarrow_array(sf::st_sfc(bx(0, 0, 2, 2)))
y <- geoarrow::as_geoarrow_array(sf::st_sfc(bx(1, 1, 3, 3)))

sf::st_area(sf::st_as_sfc(geoarrow::as_geoarrow_vctr(
  boolean_intersection(x, y)
)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/bool_ops/trait.BooleanOps.html}{BooleanOps}
}
\seealso{
Other boolean:
\code{\link[=unary_union]{unary_union()}}
}
\concept{boolean}
