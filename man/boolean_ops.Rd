% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_boolean_intersection}
\alias{ga_boolean_intersection}
\alias{ga_boolean_union}
\alias{ga_boolean_difference}
\alias{ga_boolean_xor}
\title{Combine two polygon arrays with a set operation}
\usage{
ga_boolean_intersection(x, y)

ga_boolean_union(x, y)

ga_boolean_difference(x, y)

ga_boolean_xor(x, y)
}
\arguments{
\item{x}{a GeoArrow polygon or multipolygon array}

\item{y}{a GeoArrow polygon or multipolygon array; length 1 or the same
length as \code{x}}
}
\value{
a GeoArrow multipolygon array of the same length as \code{x}
}
\description{
Each function pairs \code{x} with \code{y} row by row and returns the polygonal
result. \code{y} is recycled against \code{x}.
}
\details{
\code{ga_boolean_intersection()} keeps the area in both, \code{ga_boolean_union()} the area
in either, \code{ga_boolean_difference()} the area in \code{x} but not \code{y}, and
\code{ga_boolean_xor()} the area in exactly one of them.

These are defined for polygons and multipolygons only. A row whose geometry
is any other type, or is null, comes back null. The result is always a
multipolygon, since a set operation can split one polygon into several or
erase it entirely.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
x <- geoarrow::as_geoarrow_array(sf::st_sfc(
  sf::st_polygon(list(rbind(c(0, 0), c(2, 0), c(2, 2), c(0, 2), c(0, 0))))
))
y <- geoarrow::as_geoarrow_array(sf::st_sfc(
  sf::st_polygon(list(rbind(c(1, 1), c(3, 1), c(3, 3), c(1, 3), c(1, 1))))
))

# two 2x2 squares offset by 1, so the overlap is a 1x1 square
as.vector(ga_unsigned_area(ga_boolean_intersection(x, y)))
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/bool_ops/trait.BooleanOps.html}{BooleanOps}
}
\seealso{
Other boolean:
\code{\link[=ga_unary_union]{ga_unary_union()}}
}
\concept{boolean}
