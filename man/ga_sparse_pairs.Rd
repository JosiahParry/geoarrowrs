% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_sparse_pairs}
\alias{ga_sparse_pairs}
\title{Expand a sparse predicate into the row pairs a join needs}
\usage{
ga_sparse_pairs(hits, left = FALSE)
}
\arguments{
\item{hits}{a list array from one of the sparse predicates}

\item{left}{whether to keep the rows of \code{x} that matched nothing}
}
\value{
a struct array of \code{x} and \code{y} row numbers
}
\description{
Turns the list of matches each sparse predicate returns into two columns,
one row per pair, which is the shape \code{\link[=ga_join]{ga_join()}} takes and the shape a
database returns a spatial join in.
}
\details{
Everything but the padding is expressible with Arrow's own \code{list_flatten()}
and \code{list_parent_indices()}. What they cannot do is \code{left = TRUE}, which has
to put back a row for each row of \code{x} that matched nothing, so the whole
expansion happens here rather than half here and half in R.

Rows are 1 based, matching what the sparse predicates return. A row of \code{x}
that matched nothing appears once with a null \code{y} when \code{left = TRUE}, and
not at all otherwise.
}
\examples{
\dontshow{if (requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
x <- ga_xy(c(0, 5, 1), c(0, 5, 1))
y <- ga_xy(c(0, 1), c(0, 1))

# the middle row of x matches nothing, so it is only there with left = TRUE
ga_sparse_pairs(ga_sparse_intersects(x, y))
ga_sparse_pairs(ga_sparse_intersects(x, y), left = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other topology:
\code{\link[=ga_contains]{ga_contains()}},
\code{\link[=ga_coordinate_position]{ga_coordinate_position()}},
\code{\link[=ga_dimension]{ga_dimension()}},
\code{\link[=ga_filter]{ga_filter()}},
\code{\link[=ga_is_empty]{ga_is_empty()}},
\code{\link[=ga_join]{ga_join()}},
\code{\link[=ga_relate]{ga_relate()}},
\code{\link[=ga_sparse_intersects]{ga_sparse_intersects()}}
}
\concept{topology}
