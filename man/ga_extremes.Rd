% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_extremes}
\alias{ga_extremes}
\title{Compute the extreme coordinates of geometries}
\usage{
ga_extremes(x)
}
\arguments{
\item{x}{a GeoArrow geometry array}
}
\value{
an Arrow struct array of four point fields, with one row per geometry
}
\description{
Returns a struct array with four point fields -- \code{x_min}, \code{x_max}, \code{y_min}
and \code{y_max} -- giving the coordinate that is furthest in each direction. The
result has one row per input geometry; a null or empty geometry yields a row
of four nulls.
}
\details{
Note these are the extreme \emph{coordinates}, not the corners of the bounding
box: the \code{x_min} point carries the y value of whichever vertex was
leftmost. Use \code{\link[=ga_bounding_rect]{ga_bounding_rect()}} for the envelope.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE) && requireNamespace("geoarrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fp <- system.file("shape/nc.shp", package = "sf")
nc <- as.data.frame(read_shapefile(fp))

# one row per county, four point columns
head(as.data.frame(ga_extremes(nc$geometry)), 3)
\dontshow{\}) # examplesIf}
}
\references{
\href{https://docs.rs/geo/latest/geo/algorithm/extremes/trait.Extremes.html}{Extremes}
}
\seealso{
Other boundary:
\code{\link[=ga_bounding_rect]{ga_bounding_rect()}},
\code{\link[=ga_concave_hull]{ga_concave_hull()}},
\code{\link[=ga_convex_hull]{ga_convex_hull()}},
\code{\link[=ga_minimum_rotated_rect]{ga_minimum_rotated_rect()}}
}
\concept{boundary}
