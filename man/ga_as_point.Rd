% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{ga_as_point}
\alias{ga_as_point}
\alias{ga_as_linestring}
\alias{ga_as_polygon}
\alias{ga_as_multipoint}
\alias{ga_as_multilinestring}
\alias{ga_as_multipolygon}
\title{Cast to one GeoArrow geometry type}
\usage{
ga_as_point(x)

ga_as_linestring(x)

ga_as_polygon(x)

ga_as_multipoint(x)

ga_as_multilinestring(x)

ga_as_multipolygon(x)
}
\arguments{
\item{x}{a binary array of WKB, or any GeoArrow array}
}
\value{
a GeoArrow array of the named type, the same length as \code{x}
}
\description{
Each of these casts to the single type its name gives. Because the result
type does not depend on the data, these are the casts Arrow can run inside
a query, which \code{\link[=ga_from_wkb]{ga_from_wkb()}} cannot.
}
\details{
Parsing WKB is the expensive part of using a plain Parquet geometry column,
and every function that needs a concrete geometry type pays for it on its
own. Casting once up front means the rest of the query runs on native
GeoArrow instead:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{trip |>
  mutate(pickup = ga_as_point(t_pickuploc)) |>
  mutate(
    d = ga_dist_euclidean_pairwise(pickup, dropoff),
    b = ga_bearing_euclidean(pickup, dropoff)
  )
}\if{html}{\out{</div>}}

The cast is fallible. A geometry that does not fit the target type, such as
a two point multipoint cast to \code{point}, is an error rather than a null.
}
\examples{
\dontshow{if (requireNamespace("geoarrow", quietly = TRUE) && requireNamespace("sf", quietly = TRUE) && requireNamespace("wk", quietly = TRUE) && requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
sfc <- sf::st_sfc(sf::st_point(c(0, 0)), sf::st_point(c(3, 4)))
wkb <- nanoarrow::as_nanoarrow_array(
  arrow::Array$create(unclass(wk::as_wkb(sfc)), type = arrow::binary())
)

pts <- ga_as_point(wkb)
sf::st_as_sfc(geoarrow::as_geoarrow_vctr(pts))
\dontshow{\}) # examplesIf}
}
\seealso{
Other cast:
\code{\link[=ga_cast_geometry]{ga_cast_geometry()}},
\code{\link[=ga_downcast_geometry]{ga_downcast_geometry()}},
\code{\link[=ga_explode]{ga_explode()}},
\code{\link[=ga_flatten]{ga_flatten()}},
\code{\link[=ga_from_wkb]{ga_from_wkb()}}
}
\concept{cast}
